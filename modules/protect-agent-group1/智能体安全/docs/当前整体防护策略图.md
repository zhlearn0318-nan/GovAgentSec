# 当前整体防护策略图

> 对应当前 `SecurityAgent`、真实 PIGuard、Qwen3Guard、TrustRAG、RiskEngine、
> PolicyEngine 与 ToolGateway 实现。所有外部文本均按不可信数据处理；任一安全
> 组件异常均进入失败关闭，不会静默回退到关键词基线。

```mermaid
flowchart TB
    subgraph SRC["不可信输入面"]
        U["用户直接输入"]
        W["Web 内容"]
        F["文件 / 附件"]
        R["RAG / 知识库"]
        MEM["历史记忆 / 多轮上下文"]
        TO["工具返回内容"]
        MO["模型生成内容"]
    end

    U --> IN
    W --> IN
    F --> IN
    R --> IN
    MEM --> IN

    subgraph INPUT["第一道：请求入口防护"]
        IN["AgentRequest 边界校验<br/>去首尾空白 · 非空 · 长度限制 · 来源枚举"]
        GR["GuardRouter<br/>统一调用、类型校验、异常失败关闭"]
        PI["真实 PIGuard<br/>提示注入概率 · 阈值 0.5 · 最多 512 tokens"]
        QG["真实 Qwen3Guard-Gen-0.6B<br/>Safe / Controversial / Unsafe<br/>严格解析 Safety 与 Categories"]
        IN --> GR
        GR --> PI
        GR --> QG
    end

    subgraph RAG["第二道：RAG 数据投毒防护（use_rag=true）"]
        RET["RetrieverPort<br/>限制返回数量与文档结构"]
        PROV["KnowledgeDocument 边界<br/>ID · 来源 · 信任度 · verified · 长度"]
        TR["真实 TrustRAG 第一阶段<br/>SimCSE embedding + kmeans_ngram"]
        CLEAN["仅保留 trusted_documents<br/>原文不改写"]
        RET --> PROV --> TR --> CLEAN
    end

    IN -. "可选 RAG 查询" .-> RET

    subgraph RISK["第三道：统一风险融合与策略决策"]
        ST["来源信任度<br/>user .8 · web .3 · file .5<br/>rag .6 · memory .4 · tool .3"]
        RE["RiskEngine<br/>注入 .30 · 内容 .20 · RAG .25<br/>来源 .10 · 操作 .15"]
        BOOST["风险升级规则<br/>单个高置信信号至少 HIGH<br/>多信号 / 高危操作至少 CRITICAL"]
        PE["PolicyEngine"]
        ACT{"风险等级 / 动作"}
        RE --> BOOST --> PE --> ACT
    end

    PI --> RE
    QG --> RE
    CLEAN --> RE
    ST --> RE

    ACT -->|"LOW → ALLOW"| PLAN
    ACT -->|"MEDIUM → SANITIZE"| STOP1["停止当前链路<br/>要求净化后重试"]
    ACT -->|"HIGH → ISOLATE"| STOP2["隔离内容<br/>不进入模型"]
    ACT -->|"CRITICAL → BLOCK"| STOP3["阻断请求"]

    subgraph MODEL["第四道：模型规划与受控执行"]
        PLAN["AgentModelPort.plan<br/>只接收请求 + TrustRAG 可信上下文"]
        CHOICE{"模型结构化决策"}
        RESP["RESPOND：候选回答"]
        TREQ["TOOL：结构化 ToolRequest"]
        PLAN --> CHOICE
        CHOICE -->|"RESPOND"| RESP
        CHOICE -->|"TOOL"| TREQ
    end

    subgraph TOOL["第五道：工具调用防护"]
        OPR["操作风险重新评估"]
        TG["ToolGateway<br/>注册表 · scope · permission<br/>参数白名单 · 必填字段 · 长度"]
        CONF{"高风险操作 ≥ 0.6<br/>是否已获用户确认？"}
        EXEC["执行已注册工具"]
        TOUT["工具输出边界<br/>字符串类型 · 最大长度"]
        TSCAN["工具输出重新双检<br/>PIGuard + Qwen3Guard"]
        TRISK["工具结果风险重评估"]
        FINAL["AgentModelPort.finalize<br/>只使用已筛查工具输出"]
        TREQ --> OPR --> TG --> CONF
        CONF -->|"否"| HOLD["CONFIRMATION_REQUIRED / DENIED"]
        CONF -->|"是"| EXEC --> TOUT --> TSCAN --> TRISK
        TRISK -->|"非 LOW"| STOP4["隔离工具输出"]
        TRISK -->|"LOW"| FINAL
    end

    subgraph OUTPUT["第六道：最终输出复检"]
        OBOUND["模型输出边界<br/>非空 · 字符串 · 最大 16,000 字符"]
        OSCAN["输出重新双检<br/>PIGuard + Qwen3Guard"]
        ORISK["RiskEngine + PolicyEngine"]
        OACT{"输出策略"}
        RELEASE["COMPLETED<br/>向用户返回安全结果"]
        OBOUND --> OSCAN --> ORISK --> OACT
        OACT -->|"ALLOW"| RELEASE
        OACT -->|"SANITIZE / ISOLATE / BLOCK"| STOP5["不释放模型输出"]
    end

    RESP --> OBOUND
    FINAL --> OBOUND
    MO -. "候选内容" .-> OBOUND
    TO -. "外部工具内容" .-> TOUT

    FC["FAIL CLOSED<br/>风险分数 1.0 · BLOCK / ERROR<br/>不泄露内部异常与敏感信息"]
    PI -. "加载 / 推理异常" .-> FC
    QG -. "生成 / 解析异常" .-> FC
    RET -. "检索结构异常" .-> FC
    TR -. "嵌入 / 聚类异常" .-> FC
    PLAN -. "模型协议异常" .-> FC
    TG -. "工具执行异常" .-> FC
    OBOUND -. "非法模型输出" .-> FC

    EVAL["真实安全评测专用链路<br/>每个 RAG chunk：PIGuard + Qwen3Guard<br/>最终 retained = 双 Guard 允许 ∩ TrustRAG 保留"]
    TR -. "阶段决策" .-> EVAL
    PI -. "逐 chunk 检测" .-> EVAL
    QG -. "逐 chunk 检测" .-> EVAL

    classDef source fill:#eef2f7,stroke:#64748b,color:#0f172a;
    classDef defense fill:#e0f2fe,stroke:#0284c7,color:#0c4a6e;
    classDef rag fill:#f3e8ff,stroke:#9333ea,color:#581c87;
    classDef policy fill:#fef3c7,stroke:#d97706,color:#78350f;
    classDef safe fill:#dcfce7,stroke:#16a34a,color:#14532d;
    classDef stop fill:#fee2e2,stroke:#dc2626,color:#7f1d1d;
    classDef eval fill:#ede9fe,stroke:#7c3aed,color:#4c1d95;

    class U,W,F,R,MEM,TO,MO,IN,ST source;
    class GR,PI,QG,OSCAN,TSCAN defense;
    class RET,PROV,TR,CLEAN rag;
    class RE,BOOST,PE,ACT,OPR,TG,CONF,ORISK,OACT policy;
    class RELEASE safe;
    class STOP1,STOP2,STOP3,STOP4,STOP5,HOLD,FC stop;
    class EVAL eval;
```

## 策略动作速查

| 风险等级 | 动作 | 当前行为 |
|---|---|---|
| LOW | ALLOW | 允许进入模型、工具后续链路或释放最终输出 |
| MEDIUM | SANITIZE | 停止当前链路，要求净化后重新处理 |
| HIGH | ISOLATE | 隔离不可信内容，不送入后续模型 |
| CRITICAL | BLOCK | 直接阻断 |
| 安全组件不可用 | FAIL CLOSED | 风险置为 1.0，阻断或返回通用错误 |

## 当前实现边界

- `build_real_guard_agent()` 已接入真实 PIGuard、Qwen3Guard 和 TrustRAG 第一阶段。
- 当前 Agent 的生成器仍为 `BaselineAgentModel`；生产目标 LLM 尚未接入。
- 当前运行时 RAG 分支由 TrustRAG 过滤检索文档；固定安全评测额外执行逐 chunk
  双 Guard，并以“双 Guard 允许且 TrustRAG 保留”作为最终保留条件。
- TrustRAG 第二阶段 conflict self-assessment 和答案生成依赖官方目标 LLM，当前未配置。
- 真实全量测试结论为 FAIL：Recall 100%，但良性硬负例 FPR 21%，未达到 ≤5% 门槛。
